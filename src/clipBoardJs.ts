const clipBoard: string =
  `function si(tag,cb){;[].forEach.call(document.querySelectorAll(tag),elem=>{if(cb){cb(elem)}});return document.querySelectorAll(tag).length}document.addEventListener('DOMContentLoaded',()=>{si('.code-block-container .copy-code-btn',elem=>{elem.addEventListener('click',copyCodeToClipboard,false)})});function copyCodeToClipboard(codeIdByButton){const codeId=codeIdByButton||this.dataset?.codeId||'dg';const codeElement=document.getElementById(codeId);const text=codeElement.textContent||codeElement.innerText;if(navigator.clipboard&&navigator.clipboard.writeText){navigator.clipboard.writeText(text).then(()=>{showCopyFeedback(codeId)}).catch(err=>{console.error('Failed to copy:',err);fallbackCopyToClipboard(text,codeId)});return}fallbackCopyToClipboard(text,codeId)}function fallbackCopyToClipboard(text,codeId){const textArea=document.createElement('textarea');textArea.value=text;textArea.style.position='fixed';textArea.style.left='-999999px';document.body.appendChild(textArea);textArea.select();try{document.execCommand('copy');showCopyFeedback(codeId)}catch(err){console.error('Fallback copy failed:',err)}document.body.removeChild(textArea)}function showCopyFeedback(codeId){const button=document.querySelector(\`button[data-code-id="\${ codeId }"] .copy-text\`);if(button){const originalText=button.textContent;button.textContent='Copied!';button.parentElement.style.background='#10b981';setTimeout(()=>{button.textContent=originalText;button.parentElement.style.background='#4a5568'},2000)}}`;

export default clipBoard;
